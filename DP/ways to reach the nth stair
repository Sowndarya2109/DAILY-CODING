class Solution {
    int countWays(int n) {
        if(n == 1) return 1;
        if(n == 2) return 2;
        
        int a=1, b=2, d=0;
        for(int i=3; i<=n; i++){
            d = a+b;
            a = b; 
            b = d;
        }
        return b;
    }
}
