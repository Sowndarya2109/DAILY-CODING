class Solution {
    public static int intersectSize(int a[], int b[]) {
        int count = 0;
        HashSet<Integer> set = new HashSet<>();
        for(int x : a)
        set.add(x);
        for(int x : b){
            if(set.contains(x))
            count++;
        }
        return count;
    }
}
