class Solution {
    int missingNum(int arr[]) {
        int n = arr.length+1;
        long s = (long)n*(n+1)/2;
        long sum = 0;
        for(int x : arr){
            sum += x;
        }
        return (int)(s - sum);
       
    }
}
