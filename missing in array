class Solution {
    int missingNum(int arr[]) {
        // code here
        int n = arr.length+1;
        long sum = 0;
        long expect = (long)n*(n+1)/2;
        for(int i=0; i<n-1; i++){
            sum += arr[i];
        }
        return (int)(expect - sum);
    }
}
